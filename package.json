{
  "name": "redirector",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node-dev --respawn --transpileOnly ./src/index.ts",
    "build": "tsc",
    "server": "node ./build/index.js",
    "release": "heroku container:push web -a olafkotur && heroku container:release web -a olafkotur",
    "logs": "heroku logs -a olafkotur --tail"
  },
  "dependencies": {
    "@types/express": "^4.17.2",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "typescript": "^3.7.3"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.44"
  }
}
